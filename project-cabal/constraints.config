-- avoiding extra dependencies
constraints: rere -rere-cfg
constraints: these -assoc

-- splitmix-0.1.3 is demanding a symbol GitHub's Mac runners don't admit to having
constraints: splitmix < 0.1.3 || ^>= 0.1.3.1
